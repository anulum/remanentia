[build-system]
requires = ["maturin>=1.12,<2.0"]
build-backend = "maturin"

[project]
name = "arcane-stdp"
version = "0.1.0"
description = "Rust STDP kernel for Arcane Sapience SNN"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
